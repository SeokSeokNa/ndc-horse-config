spring:
  datasource:
    url: jdbc:mysql://localhost:13306/horse-live
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

application:
  security:
    jwt:
      secret-key: test
      expiration: 86400000 # a day
      refresh-token:
        expiration: 604800000 # 7 days